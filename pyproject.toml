[project]
name = "bayessmile"
version = "0.1.0"
description = "Generalized framework for multi-model amortized Bayesian inference"
authors = [
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "notebook>=7.5.5",
]
